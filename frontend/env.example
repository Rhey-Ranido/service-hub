# Environment Configuration
# Copy this file to .env.local for local development
# For production deployments (Vercel, Render), set these as environment variables

# API Configuration
# For production: Set this to your deployed backend URL
# Example: https://your-backend.onrender.com/api
VITE_API_BASE_URL=

# Socket Configuration  
# For production: Set this to your deployed backend URL (without /api)
# Example: https://your-backend.onrender.com
VITE_SOCKET_URL=

# Render Backend URL (alternative configuration)
# If you're using Render for backend, you can set this instead of the above two
# Example: https://your-backend.onrender.com
VITE_RENDER_BACKEND_URL=

# Development Configuration
# These are used for local development and network testing
# No need to set these for production
# VITE_DEV_HOST=192.168.1.17
# VITE_DEV_PORT=3000
